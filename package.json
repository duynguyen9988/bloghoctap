{
  "name": "bloghoctap",
  "version": "1.0.0",
  "private": true,
  "description": "bloghoctap - học mọi điều từ cuộc sống",
  "scripts": {
    "dev": "hugo server",
    "build": "hugo --gc --minify",
    "search": "pagefind --site public",
    "search:dev": "pagefind --site public --serve",
    "full:build": "npm run build && npm run search"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "autoprefixer": "^10.4.21",
    "pagefind": "^1.3.0",
    "postcss": "^8.5.3",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.17"
  },
  "browserslist": [
    "> 0.5%",
    "last 2 versions",
    "not dead"
  ]
}